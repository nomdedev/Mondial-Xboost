# Auditoría de calidad — 2026-06-14

## Alcance

Revisión completa del pipeline canónico Python antes de re-entrenar el modelo de producción.

## Hallazgos críticos corregidos

| # | Hallazgo | Impacto | Fix |
|---|----------|---------|-----|
| 1 | `tests/test_api.py` y `tests/test_xgboost_engine.py` guardaban modelos con nombre canónico `xgboost_football` | Sobreescribían el modelo de producción durante CI | Usar nombres de test (`test_api_model`, `test_xgboost_model`) y `XGBOOST_MODEL_NAME` |
| 2 | Duplicados contradictorios en `historical_results.csv` (Tahiti vs New Caledonia 1974-02-17; Gibraltar vs Cayman Islands 2026-06-06) | Data leakage / scores inconsistentes | Corregido score a 1-2 y eliminado duplicado exacto |
| 3 | `compute_elo_ratings` sumaba +100 Elo siempre | Ventaja de localía en partidos neutrales | Home advantage +100 solo si `neutral=False`; `neutral` agregado como feature |
| 4 | `.gitignore` no ignoraba entornos virtuales ni modelos binarios | Riesgo de commitear artefactos grandes | Agregados `venv/`, `.venv/`, `*.pkl`, cachés |
| 5 | CI no instalaba `requirements-dev.txt` | Jobs Python fallaban al correr pytest/ruff | Instalación de dev deps en `.github/workflows/ci.yml` |
| 6 | `scripts/run_tests.py` lintaba solo una lista parcial | 167 errores ocultos | Lint completo sobre `predictors/`, `scripts/`, `backtest/`, `tests/` |
| 7 | Documentación decía 23 features y 5 algoritmos | README desactualizado | Actualizado a 22 features y motor XGBoost único |
| 8 | Código planificado no integrado sin documentar | Confusión sobre qué está activo | Documentado en `experimental-components.md` |
| 9 | Sin manifest de modelos | Imposible auditar modelo en producción | Creado `predictors/model_manifest.py` y `data/models/model_manifest.json` |

## Cambios de datos

- Filas en `historical_results.csv`: 49,445 → 49,443
- Validación añadida en `load_historical_results()`: detecta duplicados con scores distintos.

## Cambios de código

- `predictors/feature_engineering.py`: Elo neutral fix, validación de duplicados, feature `neutral`.
- `predictors/xgboost_engine.py`: integración con `model_manifest.py`.
- `predictors/model_manifest.py`: nuevo helper para reproducibilidad.
- `scripts/train.py`: métricas de entrenamiento en manifest.
- `scripts/run_tests.py`: lint completo.
- `.github/workflows/ci.yml`: dependencias de desarrollo.
- `.gitignore`: entornos y modelos.

## Métricas de entrenamiento (post-fix)

Ver `data/models/model_manifest.json`.

## Validación ejecutada

- `ruff check predictors scripts backtest tests` → 0 errores
- `pytest tests/` → 11 passed
- `scripts/verify_gates.py` → 9 PASS / 3 SKIP (.NET no disponible en este entorno)
- `scripts/train.py` → modelo re-entrenado con manifest

## Próximos pasos recomendados

1. Validar gates .NET en entorno con .NET 9 (`dotnet build`, `dotnet format`, `dotnet test`).
2. Ejecutar backtest temporal honesto y reportar métricas de test en el manifest.
3. Evaluar integración de `player_weights.py`, `llm_analysis_service.py` y `scrapers/` con métricas de mejora.
4. Automatizar la comparación de `model_manifest.json` en CI para detectar drift de features.
