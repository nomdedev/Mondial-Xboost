# Development dependencies for Mondial-Xboost
# Install with: pip install -r requirements.txt -r requirements-dev.txt

ruff>=0.6.0
pytest>=8.0.0
httpx>=0.27.0          # for FastAPI TestClient
pip-audit>=2.7.0       # dependency vulnerability scanning
